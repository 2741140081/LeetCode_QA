08-18: 
1. 添加功能按钮, 上一章, 下一章, 自动播放
2. 在 test_reader.fxml 中的左侧添加章节目录窗口, 从数据库读取章节信息, 并且点击任意章节后, 会在右侧查看该章节的漫画内容
3. 自动播放功能: 点击后显示/激活隐藏按钮, 加速/减速, 并且当自动播放到最后一张图片后, 自动跳转到下一章, 如果没有下一章则退出自动播放模式.

需要的操作
- 在 mysql 数据库中构建相关的表, 并且当前只需要一本漫画和10个左右的章节即可
- 构建 Mapper 文件, 从数据库获取漫画信息, 包括章节信息和各个章节的图片信息
- 修改 test_reader.fxml 文件, 添加章节目录窗口和功能按钮
- 添加service 接口和相关的 controller, 完善代码




08-19:
进度: 目前已经构建基本框架, 包含数据库查询, 已经相关页面
存在问题:
1. 自动播放无法到达最后一张图片后, 无法自动跳转到下一章
2. 自动播放时跳跃的间隔太大了, 基本相当于是图片的切换, 而非自然的滚动

后续安排:
1. 添加我的书架, 内容为待阅读的漫画, 类似于小说的书架, 封面 + 漫画名称,
排列是九宫格的布局方式.
2. 并且现在会有两个 fxml 文件, 首先加载我的书架界面, 然后通过点击的方式,
跳转到漫画阅读界面, 并且整个 app 应该仅存在一个界面而非两个界面, 待验证可行性.


08-20:
仍然没有解决自动播放的问题, 当前存在的问题是自动播放过程中抖动太严重了, 暂时性放弃
1. 目前已经完成漫画书架的基础搭建和跳转

后续安排:
1. 构建一个漫画下载中心页面, 使用爬虫从网络中爬取相关的漫画
2. 构建页面需要的参数, 漫画名称, 漫画目录网址, 下载线程数量。

具体操作步骤
1. 通过漫画目录网址获取网页源码
2. 得到源码后解析目录, 得到目录后可以参考

